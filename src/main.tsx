import React from 'react'
import ReactDOM from 'react-dom/client'
import VolumeApp from './VolumeApp.tsx'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <VolumeApp />
  </React.StrictMode>,
)
